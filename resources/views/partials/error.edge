@if(flashMessages.has('error'))
  <div class="alert alert-danger w-100" role="alert">
    {{ flashMessages.get('error') }}
  </div>
@endif

@if(flashMessages.has('errors'))
  <div class="alert alert-danger w-100" role="alert">
    لطفا ورودی های خود را بررسی نمایید
  </div>
@endif